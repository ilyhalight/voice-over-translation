// Import from new subdirectory structure.
// This file exists so CRXJS can resolve the entry point at the expected path.
// Side-effects only: bootstrapExtensionBridge() is called at the end of the module.
import "./bridge/index";
